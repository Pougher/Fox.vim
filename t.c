#include <stdio.h>
#define MAXN 100

// this is the right colour
typedef struct {
    char* name = "axolotlfox";
} Fox_t;

int main(int fd = 0) { 
    case 0: break;
    switch() {}
    while() {}
    for() {}
    if() {}
    else() {}
    do() {}
    printf("%d\n", fd);
    return 0 * 20;
}

// hello world in assembly
